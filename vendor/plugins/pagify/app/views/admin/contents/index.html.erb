
<h1>Versiones de la página '<%= @page.title %>'</h1>

<table>
  <tr>
    <td>Modificado</td>
    <td></td>
  </tr>
  
  <% for content in @contents %>
    <tr>
      <td><%= content.updated_at %></td>
      <td>
        <%= link_to 'Show', [@project, @page, content] %>
        <%= link_to 'Edit', [@project, @page, content, :edit] %>
        <%= link_to 'Destroy', [@project, @page, content], :confirm => 'Are you sure?', :method => :delete %>
      </td>
    </tr>
  <% end %>
</table>

<br />

